[pytest]
markers =
    quick: Fast tests using synthetic or demo_small fixtures (under 10s total)
    regression: Regression tests using 100k benchmark files (~30s)
    benchmark: Milestone tests using 500k benchmark files (~5 min)
addopts = -v
testpaths = tests
pythonpath = .
